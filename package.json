{
  "name": "ts-paths",
  "version": "0.1.3",
  "description": "transform tsconfig.json paths",
  "main": "ts-paths",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "yarn clean && npx tsc",
    "dev": "cd .. && cd lenneth && node ../ts-paths/lib build ./ -t tsconfig.json -d -l 2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "ts-paths": "ts-paths"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/chalk": "^2.2.0",
    "@types/commander": "^2.12.2",
    "@types/fs-extra": "^5.0.2",
    "@types/node": "^10.3.0",
    "@types/shelljs": "^0.8.0",
    "typescript": "^2.9.1"
  },
  "keywords": [
    "typescript",
    "node",
    "paths",
    "tsc",
    "tsc paths"
  ],
  "dependencies": {
    "chalk": "^2.4.1",
    "commander": "^2.15.1",
    "fs-extra": "^6.0.1",
    "reflect-metadata": "^0.1.12",
    "shelljs": "^0.8.2",
    "tslib": "^1.9.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/soraping/ts-paths.git"
  },
  "bugs": {
    "url": "https://github.com/soraping/ts-paths/issues"
  },
  "homepage": "https://github.com/soraping/ts-paths#readme",
  "engines": {
    "node": ">=8.0.0"
  },
  "author": {
    "name": "caoshiping",
    "email": "soraping@163.com",
    "url": "https://github.com/soraping"
  }
}
